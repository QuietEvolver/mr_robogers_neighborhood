// Buisiness Logic 

(function beepBoop(){
  let input = [5, 1];
  let countUpByIntake = [];
  console.log("countUpByIntakeIntake",countUpByIntake); 
  for( let i = 0; i <= input[0]; i += input[1]){
      if (1 === i) {
        message = "Beep!";
      countUpByIntake.push(message);
      } else{
          countUpByIntake.push(i);
        // turn i into a Str then push? no it comes out.
      }  
  }
})();
